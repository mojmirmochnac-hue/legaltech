# legaltech

Projekt je pripravený na prepojenie s Netlify projektom `fascinating-puffpuff-0bd3fd` cez lokálny stav v `.netlify/state.json`.

## Aktuálne nastavenie
- `.netlify/state.json` obsahuje `siteId` nastavené na `fascinating-puffpuff-0bd3fd`.
- `netlify.toml` je pridaný s minimálnou build konfiguráciou.

## Overenie (keď bude dostupné Netlify CLI + token)
```bash
netlify status
netlify deploy --prod
```
